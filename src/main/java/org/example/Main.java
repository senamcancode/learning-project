package org.example;

import org.example.code_wars.FindOdd;
import org.example.code_wars.GetMiddle;
import org.example.neetcode.IsPalindrome;
import org.example.neetcode.IsPalindrome;
import org.example.neetcode.RemoveElementClass;
import org.example.neetcode.ReverseArray;
import org.example.singleton_pattern.Eager_Singleton;
import org.example.singleton_pattern.Lazy_Singleton;
import org.example.singleton_pattern.Logger;
import org.example.strategy_pattern.GameAttacks.AttackAction;
import org.example.strategy_pattern.GameAttacks.Magic;
import org.example.strategy_pattern.SortingStrategy.BubbleSort;
import org.example.strategy_pattern.SortingStrategy.MergeSort;
import org.example.strategy_pattern.SortingStrategy.QuickSort;
import org.example.strategy_pattern.SortingStrategy.Sorter;


//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
//       int a = 4;
//       int b = 9;
//       int c = 10;
//
//       Sorter sorter = new Sorter();
//       sorter.setStrategy(new BubbleSort());
//       sorter.executeSort(a, b, c);
//
//       sorter.setStrategy(new MergeSort());
//       sorter.executeSort(a,b,c);
//
//
//       sorter.setStrategy(new QuickSort());
//       sorter.executeSort(a,b,c);
//
//
//       AttackAction attack = new AttackAction();
//       attack.setStrategy(new Magic());
//
//       attack.executeAttack();
//
//       Logger logger = Logger.getLoggerInstance();
//
//       logger.printingExample();
//
//
//       Lazy_Singleton.getInstance();
//
//       Eager_Singleton.getInstance();


//       FindOdd.findIt(new int[] {20,1,-1,2,-2,3,3,5,5,1,2,4,20,4,-1,-2});

//        GetMiddle.getMiddle("senam");

//        RemoveElementClass removeElementTest = new RemoveElementClass();

//        int [] testArray = new int[] {1,2,3,4};
//
//        ReverseArray reverseTest = new ReverseArray();
//
//        reverseTest.reverseArray(testArray);

        IsPalindrome isPallindrome = new IsPalindrome();
        isPallindrome.isPalindrome("Hannah");





    }
}

